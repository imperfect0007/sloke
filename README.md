# sloke

Backend API: see [`slooze-api/README.md`](slooze-api/README.md) for setup, scripts, GraphQL usage, and **Vercel** deploy steps.

When importing into Vercel, set **Root Directory** to `slooze-api`.
